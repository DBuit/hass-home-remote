import { ScreensaverTimerDirective } from './screensaver-timer.directive';

describe('ScreensaverTimerDirective', () => {
  it('should create an instance', () => {
    const directive = new ScreensaverTimerDirective();
    expect(directive).toBeTruthy();
  });
});
